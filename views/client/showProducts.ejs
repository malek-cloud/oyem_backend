
<%- include('../includes/head.ejs')%>
<body>
    <%- include('../includes/navigation.ejs')%>
    <main>
        <% if(prods.length > 0) {%>
        <ul>
            <%  for (let prod of prods){ %>
                
                <li>
                    <%= prod.title %>
                    <a href='/detail/<%= prod._id %>'> show in details</a>

                </li>
                <% }%>
        </ul>
     <% }else{ %>
        <h1>No Products to display</h1>
        <% } %>
    </main>
</body>

</html>